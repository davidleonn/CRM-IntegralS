<!DOCTYPE html>
<html lang="en">
<head>
 <%- include('../partials/head'); %>
 <title>Dashboard</title>
 <style>
    a {
       color:black;
       text-decoration:none;
    }
</style>
</head>

  <body>

    <%- include('../partials/headerUser'); %>
    <div class="container-fluid">
        <div class="row">
          <%- include('../partials/sideNavbar'); %>
  
          <main role="main" class="col-md-9 ml-sm-auto col-lg-10 pt-3 px-4"><div class="chartjs-size-monitor" style="position: absolute; inset: 0px; overflow: hidden; pointer-events: none; visibility: hidden; z-index: -1;"><div class="chartjs-size-monitor-expand" style="position:absolute;left:0;top:0;right:0;bottom:0;overflow:hidden;pointer-events:none;visibility:hidden;z-index:-1;"><div style="position:absolute;width:1000000px;height:1000000px;left:0;top:0"></div></div><div class="chartjs-size-monitor-shrink" style="position:absolute;left:0;top:0;right:0;bottom:0;overflow:hidden;pointer-events:none;visibility:hidden;z-index:-1;"><div style="position:absolute;width:200%;height:200%;left:0; top:0"></div></div></div>
            <h2>Expenses report</h2>
            <div class="table-responsive">
              <table class="table table-striped table-sm">
                <thead>
                  <tr>
                    <th>Expense ID</th>
                    <th>Company Name</th>
                    <th>Product name</th>
                    <th>Quantity </th>
                    <th>Product ID</th>
                    <th>Product Unit Price</th>
                    <th>Totat expense ($) </th>
                  </tr>
                </thead>
                <% if  (expenses.length > 0) { %>
                  <% expenses.forEach (expense => { %>
                    <tbody>
                      <tr>
                        <td><%= expense._id %></td>
                        <td><%= expense.companyName %></td>
                        <td><%= expense.productName %></td>
                        <td><%= expense.productQuantity %></td>
                        <td><%= expense.productID %></td>
                        <td><%= expense.productPrice %></td>
                        <td><%=  (expense.productPrice * expense.productQuantity) %></td>
                      </tr>
                    </tbody>
              <% }) %> 
              <% } else { %>
                 <h1> There are no expenses :(</h1>
                 <% } %> 
              
              </table>
            </div>

            
        </main>
        </div>
      </div>


      <%- include('../partials/footer'); %>
    

  </body>
</html>